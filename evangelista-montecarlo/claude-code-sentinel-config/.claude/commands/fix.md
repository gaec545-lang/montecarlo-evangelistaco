MODO FIX AUTÓNOMO para: $ARGUMENTS

## PROTOCOLO:

1. **Reproduce** el error — crea un test mínimo que lo demuestre
2. **Diagnostica** — lee el stack trace, revisa el código, identifica root cause
3. **Corrige** — implementa el fix más limpio posible
4. **Protege** — agrega test unitario que cubra este caso exacto
5. **Verifica** — corre la suite completa de tests para confirmar que nada se rompió
6. **Documenta** — agrega comentario `# FIX: [descripción]` y entrada en CHANGELOG

NO me preguntes nada. Si el fix requiere cambiar algo de la lista protegida (Sección 1.1 del CLAUDE.md), ahí sí detente y explícame la situación con tu propuesta de solución.

Si el bug tiene múltiples causas posibles, investiga todas antes de elegir. Elige la causa raíz, no el síntoma.
